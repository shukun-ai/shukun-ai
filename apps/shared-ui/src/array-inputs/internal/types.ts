export type DroppableItem = {
  sourceIndex: number;
};

export const EVENT_NODE_TYPE = 'EVENT_NODE_TYPE';

export const ACTIVE_DROPPABLE_HEIGHT = 20;
export const INACTIVE_DROPPABLE_HEIGHT = 20;
export const LEFT_INDENT_WIDTH = 0;
